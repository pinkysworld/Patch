import fs from 'node:fs';
import { parseStudioProjectBundle, serializeStudioProjectBundle } from '../src/studio-project.js';

function edit(path, transform) {
  const before = fs.readFileSync(path, 'utf8');
  const after = transform(before);
  if (after === before) throw new Error(`No change made to ${path}`);
  fs.writeFileSync(path, after);
}
function once(source, before, after, label) {
  const count = source.split(before).length - 1;
  if (count !== 1) throw new Error(`Expected one anchor for ${label}, found ${count}`);
  return source.replace(before, after);
}

edit('src/window-layout-policy.js', source => {
  let s = source;
  s = once(s, "export const PATCH_SLIDER_PRESENTATION_VERSION = '0.1';", "export const PATCH_SLIDER_PRESENTATION_VERSION = '0.2';", 'slider presentation version');
  s = once(s, "export const PATCH_WINDOW_SLIDER_PRESENTATION_VERSION = '0.1';", "export const PATCH_WINDOW_SLIDER_PRESENTATION_VERSION = '0.2';", 'window slider presentation version');
  s = once(s, "const SLIDER_PRESENTATION_MODES = Object.freeze(['plain', 'progress']);", "const SLIDER_PRESENTATION_MODES = Object.freeze(['plain', 'progress', 'scrollbar']);", 'slider presentation modes');
  s = once(s, "const PROGRESS_SLIDER_TARGETS = Object.freeze({ studio: 'supported', web: 'supported', windows: 'unsupported', macos: 'unsupported', linux: 'unsupported', freebsd: 'unsupported' });", "const PROGRESS_SLIDER_TARGETS = Object.freeze({ studio: 'supported', web: 'supported', windows: 'unsupported', macos: 'unsupported', linux: 'unsupported', freebsd: 'unsupported' });\nconst SCROLLBAR_SLIDER_TARGETS = Object.freeze({ studio: 'supported', web: 'supported', windows: 'unsupported', macos: 'unsupported', linux: 'unsupported', freebsd: 'unsupported' });", 'scrollbar target matrix');
  s = once(s, "if (!SLIDER_PRESENTATION_MODE_SET.has(normalized)) throw new Error(`Unsupported Slider presentation '${mode}'. Use plain or progress.`);", "if (!SLIDER_PRESENTATION_MODE_SET.has(normalized)) throw new Error(`Unsupported Slider presentation '${mode}'. Use plain, progress or scrollbar.`);", 'slider normalize error');
  s = once(s, "const match = text.match(/^\\s*#\\s*@slider-mode\\s+(plain|progress)\\s*$/i);\n  if (!match) throw new Error(`Invalid # @slider-mode directive '${text.trim()}'. Use '# @slider-mode progress'.`);", "const match = text.match(/^\\s*#\\s*@slider-mode\\s+(plain|progress|scrollbar)\\s*$/i);\n  if (!match) throw new Error(`Invalid # @slider-mode directive '${text.trim()}'. Use '# @slider-mode progress' or '# @slider-mode scrollbar'.`);", 'slider parser modes');
  s = once(s, "export function patchSliderPresentationTargetSupport(mode) { return normalizePatchSliderPresentation(mode) === 'progress' ? PROGRESS_SLIDER_TARGETS : PLAIN_SLIDER_TARGETS; }", "export function patchSliderPresentationTargetSupport(mode) {\n  const normalized = normalizePatchSliderPresentation(mode);\n  if (normalized === 'progress') return PROGRESS_SLIDER_TARGETS;\n  if (normalized === 'scrollbar') return SCROLLBAR_SLIDER_TARGETS;\n  return PLAIN_SLIDER_TARGETS;\n}", 'slider target support');
  s = once(s, "(normalizedMode === 'progress' ? 'ProgressBar Stage 1 is Studio/Web only until a new explicit native GUI/runtime contract is promoted.' : 'Select a supported Patch target.')", "(normalizedMode === 'progress' ? 'ProgressBar Stage 1 is Studio/Web only until a new explicit native GUI/runtime contract is promoted.' : normalizedMode === 'scrollbar' ? 'ScrollBar Stage 1 is Studio/Web only until a new explicit native GUI/runtime presentation contract is promoted.' : 'Select a supported Patch target.')", 'slider target message');
  s = once(s, "export function collectWindowProgressBarIds(ast) {\n  const ids = [];\n  walkWindowControls(ast, node => { if (node.control === 'slider' && node.sliderPresentation === 'progress' && node.id) ids.push(node.id); });\n  return ids;\n}", "export function collectWindowProgressBarIds(ast) {\n  const ids = [];\n  walkWindowControls(ast, node => { if (node.control === 'slider' && node.sliderPresentation === 'progress' && node.id) ids.push(node.id); });\n  return ids;\n}\n\nexport function collectWindowScrollBarIds(ast) {\n  const ids = [];\n  walkWindowControls(ast, node => { if (node.control === 'slider' && node.sliderPresentation === 'scrollbar' && node.id) ids.push(node.id); });\n  return ids;\n}", 'scrollbar id collector');
  return s;
});

edit('src/slider-presentation.js', source => once(source, '  collectWindowProgressBarIds\n', '  collectWindowProgressBarIds,\n  collectWindowScrollBarIds\n', 'slider re-export'));

edit('src/window-build.js', source => {
  let s = source;
  s = once(s, '  let progressBars = 0;\n  let memos = 0;', '  let progressBars = 0;\n  let scrollBars = 0;\n  let memos = 0;', 'window scrollbar counter');
  s = once(s, "      if (child.sliderPresentation === 'progress') progressBars += 1;", "      if (child.sliderPresentation === 'progress') progressBars += 1;\n      if (child.sliderPresentation === 'scrollbar') scrollBars += 1;", 'window scrollbar count');
  s = once(s, "  if (progressBars && !options.allowProgressBar) {\n    throw new WindowBuildError(\n      'ProgressBar Stage 1 is enabled only for Patch Studio and Standalone Window Web. Current Ready native GUI 1.9/19/1.10 has no passive ProgressBar presentation contract; validation fails closed rather than lowering it as an interactive Slider.'\n    );\n  }", "  if (progressBars && !options.allowProgressBar) {\n    throw new WindowBuildError(\n      'ProgressBar Stage 1 is enabled only for Patch Studio and Standalone Window Web. Current Ready native GUI 1.9/19/1.10 has no passive ProgressBar presentation contract; validation fails closed rather than lowering it as an interactive Slider.'\n    );\n  }\n\n  if (scrollBars && !options.allowScrollBar) {\n    throw new WindowBuildError(\n      'ScrollBar Stage 1 is enabled only for Patch Studio and Standalone Window Web. Current Ready native GUI 1.9/19/1.10 has no standalone ScrollBar presentation contract; validation fails closed rather than lowering it as an ordinary Slider.'\n    );\n  }", 'window scrollbar target gate');
  s = once(s, '    progressBars,\n    memos,', '    progressBars,\n    scrollBars,\n    memos,', 'window scrollbar result');
  return s;
});

edit('src/window-compiled.js', source => once(source, '    allowProgressBar: true,\n    allowMemo: true,', '    allowProgressBar: true,\n    allowScrollBar: true,\n    allowMemo: true,', 'target-neutral scrollbar allowance'));

edit('src/native-current-contract.js', source => once(
  source,
  "    if (node?.kind === 'uiControl' && node.control === 'slider' && node.sliderPresentation === 'progress') {\n      const name = node.id ? ` '${node.id}'` : '';\n      throw new NativeGuiError(`ProgressBar Stage 1${name} is Studio/Web only. Current Ready native ${PATCH_CURRENT_NATIVE_RUNTIME_VERSION} has no passive progress presentation contract; validation fails closed rather than lowering it as an interactive Slider.`);\n    }",
  "    if (node?.kind === 'uiControl' && node.control === 'slider' && node.sliderPresentation === 'progress') {\n      const name = node.id ? ` '${node.id}'` : '';\n      throw new NativeGuiError(`ProgressBar Stage 1${name} is Studio/Web only. Current Ready native ${PATCH_CURRENT_NATIVE_RUNTIME_VERSION} has no passive progress presentation contract; validation fails closed rather than lowering it as an interactive Slider.`);\n    }\n    if (node?.kind === 'uiControl' && node.control === 'slider' && node.sliderPresentation === 'scrollbar') {\n      const name = node.id ? ` '${node.id}'` : '';\n      throw new NativeGuiError(`ScrollBar Stage 1${name} is Studio/Web only. Current Ready native ${PATCH_CURRENT_NATIVE_RUNTIME_VERSION} has no standalone scrollbar presentation contract; validation fails closed rather than lowering it as an ordinary native Slider.`);\n    }",
  'native scrollbar fail closed'
));

edit('web/slider-stage1.js', source => {
  let s = source;
  s = once(s, ".patch-slider.patch-progressbar-studio.designer-control{cursor:pointer}\n@media(forced-colors:active){.patch-slider.patch-progressbar-studio progress{forced-color-adjust:auto}}", `.patch-slider.patch-progressbar-studio.designer-control{cursor:pointer}\n.patch-slider.patch-scrollbar-studio{display:grid;grid-template-columns:minmax(0,1fr);align-items:center;min-width:180px;padding:4px 2px}\n.patch-slider.patch-scrollbar-studio input[type="range"]{grid-column:1;width:100%;height:16px;margin:0;padding:0;border:0;border-radius:999px;background:#e4e4e7;appearance:none;-webkit-appearance:none}\n.patch-slider.patch-scrollbar-studio input[type="range"]::-webkit-slider-thumb{width:48px;height:14px;border:1px solid #71717a;border-radius:999px;background:#a1a1aa;-webkit-appearance:none}\n.patch-slider.patch-scrollbar-studio input[type="range"]::-moz-range-thumb{width:48px;height:14px;border:1px solid #71717a;border-radius:999px;background:#a1a1aa}\n.patch-slider.patch-scrollbar-studio input[type="range"]::-moz-range-track{height:16px;border:0;border-radius:999px;background:#e4e4e7}\n.patch-slider.patch-scrollbar-studio>output,.patch-slider.patch-scrollbar-studio>.patch-slider-range{position:absolute!important;width:1px!important;height:1px!important;min-width:1px!important;min-height:1px!important;margin:-1px!important;padding:0!important;border:0!important;clip:rect(0 0 0 0)!important;clip-path:inset(50%)!important;overflow:hidden!important;white-space:nowrap!important}\n@media(prefers-color-scheme:dark){.patch-slider.patch-scrollbar-studio input[type="range"]{background:#3f3f46}.patch-slider.patch-scrollbar-studio input[type="range"]::-webkit-slider-thumb,.patch-slider.patch-scrollbar-studio input[type="range"]::-moz-range-thumb{border-color:#a1a1aa;background:#71717a}}\n@media(forced-colors:active){.patch-slider.patch-progressbar-studio progress{forced-color-adjust:auto}.patch-slider.patch-scrollbar-studio input[type="range"]{forced-color-adjust:auto}}`, 'Studio scrollbar styles');
  s = once(s, "  if (!toolbar || !sliderButton || toolbar.querySelector('#addProgressBar')) return Boolean(toolbar?.querySelector('#addProgressBar'));", "  if (!toolbar || !sliderButton) return false;\n  if (toolbar.querySelector('#addProgressBar') && toolbar.querySelector('#addScrollBar')) return true;", 'Studio toolbar early return');
  s = once(s, "  const button = document.createElement('button');", "  const button = toolbar.querySelector('#addProgressBar') ?? document.createElement('button');", 'reuse Progress button');
  s = once(s, "  sliderButton.insertAdjacentElement('afterend', button);\n  button.addEventListener('click', addProgressBarFromStudio);\n  return true;", "  if (!button.isConnected) {\n    sliderButton.insertAdjacentElement('afterend', button);\n    button.addEventListener('click', addProgressBarFromStudio);\n  }\n  let scroll = toolbar.querySelector('#addScrollBar');\n  if (!scroll) {\n    scroll = document.createElement('button');\n    scroll.id = 'addScrollBar';\n    scroll.className = 'secondary small';\n    scroll.type = 'button';\n    scroll.textContent = '+ ScrollBar';\n    scroll.setAttribute('aria-label', 'Add ScrollBar');\n    scroll.title = 'Add an interactive source-backed ScrollBar preset. It remains Slider changed(value) semantics and uses # @slider-mode scrollbar.';\n    button.insertAdjacentElement('afterend', scroll);\n    scroll.addEventListener('click', addScrollBarFromStudio);\n  }\n  return true;", 'Studio ScrollBar toolbar button');
  s = once(s, "function ensureProgressBarInspector() {", "function addScrollBarFromStudio(event) {\n  event?.preventDefault?.();\n  event?.stopImmediatePropagation?.();\n  if (!code) return;\n  try {\n    const windowIndex = activeFormIndex();\n    let next = addDesignerControl(code.value, 'slider', { windowIndex });\n    const added = listDesignerControls(next).filter(control => control.windowIndex === windowIndex && control.type === 'slider').at(-1);\n    if (!added?.id) throw new Error('Designer created a Slider but could not locate its source-backed id.');\n    const line = findSliderLineById(next, added.id);\n    next = setWindowSliderPresentation(next, line, 'scrollbar');\n    setSource(next);\n    requestAnimationFrame(() => {\n      document.querySelector(`#designerCanvas .designer-control[data-window-index=\\\"${added.windowIndex}\\\"][data-control-index=\\\"${added.controlIndex}\\\"]`)?.click?.();\n      scheduleProgressBarSync();\n    });\n  } catch (error) {\n    showDesignerInspectorError(error, { document });\n  }\n}\n\nfunction ensureProgressBarInspector() {", 'Studio ScrollBar add handler');
  s = once(s, '      <option value="progress">ProgressBar</option>\n    </select>\n    <small id="designerInspectorSliderPresentationHint" class="inspector-hint">ProgressBar is a passive source-backed number-state presentation. Stage 1 is Studio/Web; Current Ready native 1.10 fails closed.</small>', '      <option value="progress">ProgressBar</option>\n      <option value="scrollbar">ScrollBar</option>\n    </select>\n    <small id="designerInspectorSliderPresentationHint" class="inspector-hint">ProgressBar is passive; ScrollBar stays interactive with ordinary Slider changed(value). Both presentations are Studio/Web and fail closed on Current Ready native 1.10.</small>', 'Studio Slider Inspector options');
  s = once(s, "    progressIds = new Set(manifest.controls.filter(control => control.mode === 'progress').map(control => control.id).filter(Boolean));", "    progressIds = new Set(manifest.controls.filter(control => control.mode === 'progress').map(control => control.id).filter(Boolean));\n    var scrollBarIds = new Set(manifest.controls.filter(control => control.mode === 'scrollbar').map(control => control.id).filter(Boolean));", 'Studio scrollbar id collection');
  s = once(s, "      if (progressIds.has(id)) renderProgressPresentation(slider, id, root.id === 'app');\n      else restoreSliderPresentation(slider, root.id === 'app');", "      if (progressIds.has(id)) renderProgressPresentation(slider, id, root.id === 'app');\n      else if (scrollBarIds.has(id)) renderScrollBarPresentation(slider, id, root.id === 'app');\n      else restoreSliderPresentation(slider, root.id === 'app');", 'Studio scrollbar rendering dispatch');
  s = once(s, "function restoreSliderPresentation(slider, interactiveRoot) {\n  if (!slider.classList.contains('patch-progressbar-studio')) return;\n  slider.classList.remove('patch-progressbar-studio');\n  delete slider.dataset.patchSliderPresentation;", "function renderScrollBarPresentation(slider, id, interactiveRoot) {\n  restoreSliderPresentation(slider, interactiveRoot);\n  slider.classList.add('patch-scrollbar-studio');\n  slider.dataset.patchSliderPresentation = 'scrollbar';\n  const input = slider.querySelector('input[type=\\\"range\\\"]');\n  if (!input) return;\n  input.disabled = !interactiveRoot;\n  input.removeAttribute('aria-hidden');\n  input.setAttribute('aria-label', `${id} scrollbar`);\n  input.setAttribute('aria-roledescription', 'scroll bar');\n  if (interactiveRoot) input.removeAttribute('tabindex'); else input.tabIndex = -1;\n}\n\nfunction restoreSliderPresentation(slider, interactiveRoot) {\n  if (!slider.classList.contains('patch-progressbar-studio') && !slider.classList.contains('patch-scrollbar-studio')) return;\n  slider.classList.remove('patch-progressbar-studio', 'patch-scrollbar-studio');\n  delete slider.dataset.patchSliderPresentation;", 'Studio scrollbar render/restore');
  s = once(s, "    input.removeAttribute('aria-hidden');\n    input.removeAttribute('tabindex');\n    input.disabled = !interactiveRoot;", "    input.removeAttribute('aria-hidden');\n    input.removeAttribute('aria-roledescription');\n    input.removeAttribute('tabindex');\n    input.disabled = !interactiveRoot;", 'Studio scrollbar aria restore');
  s = once(s, "if (document.querySelector('#addProgressBar') && document.querySelector('#designerInspectorSliderPresentationField')) observer.disconnect();", "if (document.querySelector('#addProgressBar') && document.querySelector('#addScrollBar') && document.querySelector('#designerInspectorSliderPresentationField')) observer.disconnect();", 'Studio install observer completeness');
  return s;
});

edit('src/window-web-paintbox.js', source => {
  let s = source;
  s = once(s, "export const PATCH_WINDOW_WEB_PROGRESSBAR_VERSION = '0.1';", "export const PATCH_WINDOW_WEB_PROGRESSBAR_VERSION = '0.1';\nexport const PATCH_WINDOW_WEB_SCROLLBAR_VERSION = '0.1';", 'Web ScrollBar version');
  s = once(s, '  const progressBarIds = collectProgressBarIds(ast);\n  const hasPaintBoxes', '  const progressBarIds = collectProgressBarIds(ast);\n  const scrollBarIds = collectScrollBarIds(ast);\n  const hasPaintBoxes', 'Web scrollbar ids');
  s = once(s, '  const hasProgressBars = progressBarIds.length > 0;\n  if (!hasPaintBoxes && !hasPasswordEdits && !hasMaskedEdits && !hasCheckedListBoxes && !hasProgressBars) return built;', '  const hasProgressBars = progressBarIds.length > 0;\n  const hasScrollBars = scrollBarIds.length > 0;\n  if (!hasPaintBoxes && !hasPasswordEdits && !hasMaskedEdits && !hasCheckedListBoxes && !hasProgressBars && !hasScrollBars) return built;', 'Web scrollbar presence');
  s = once(s, "  if (hasProgressBars) {\n    html = html\n      .replace('</head>', `${progressBarStyle()}\\n</head>`)\n      .replace('</body>', `${progressBarRuntime(progressBarIds)}\\n</body>`);\n  }", "  if (hasProgressBars) {\n    html = html\n      .replace('</head>', `${progressBarStyle()}\\n</head>`)\n      .replace('</body>', `${progressBarRuntime(progressBarIds)}\\n</body>`);\n  }\n  if (hasScrollBars) {\n    html = html\n      .replace('</head>', `${scrollBarStyle()}\\n</head>`)\n      .replace('</body>', `${scrollBarRuntime(scrollBarIds)}\\n</body>`);\n  }", 'Web scrollbar enhancer');
  s = once(s, "      ...(hasProgressBars ? {\n        progressBarStage: 1,\n        progressBarVersion: PATCH_WINDOW_WEB_PROGRESSBAR_VERSION,\n        progressBarMode: 'passive-number-state-presentation'\n      } : {})", "      ...(hasProgressBars ? {\n        progressBarStage: 1,\n        progressBarVersion: PATCH_WINDOW_WEB_PROGRESSBAR_VERSION,\n        progressBarMode: 'passive-number-state-presentation'\n      } : {}),\n      ...(hasScrollBars ? {\n        scrollBarStage: 1,\n        scrollBarVersion: PATCH_WINDOW_WEB_SCROLLBAR_VERSION,\n        scrollBarMode: 'interactive-slider-scrollbar-presentation',\n        scrollBarEvent: 'changed-number'\n      } : {})", 'Web scrollbar metadata');
  s = once(s, "export function collectProgressBarIds(ast) {\n  const ids = [];\n  walk(ast, node => {\n    if (\n      node.kind === 'uiControl' &&\n      node.control === 'slider' &&\n      node.sliderPresentation === 'progress' &&\n      node.id\n    ) ids.push(node.id);\n  });\n  return ids;\n}", "export function collectProgressBarIds(ast) {\n  const ids = [];\n  walk(ast, node => {\n    if (\n      node.kind === 'uiControl' &&\n      node.control === 'slider' &&\n      node.sliderPresentation === 'progress' &&\n      node.id\n    ) ids.push(node.id);\n  });\n  return ids;\n}\n\nexport function collectScrollBarIds(ast) {\n  const ids = [];\n  walk(ast, node => {\n    if (node.kind === 'uiControl' && node.control === 'slider' && node.sliderPresentation === 'scrollbar' && node.id) ids.push(node.id);\n  });\n  return ids;\n}", 'Web scrollbar collector');
  s = once(s, 'function paintBoxStyle() {', `function scrollBarStyle() {\n  return \`<style data-patch-window-scrollbar>\n.patch-slider.patch-scrollbar{display:grid;grid-template-columns:minmax(0,1fr);align-items:center;min-width:180px;color:inherit}.patch-slider.patch-scrollbar input[type="range"]{grid-column:1;width:100%;height:16px;margin:0;padding:0;border:0;border-radius:999px;background:#e4e4e7;appearance:none;-webkit-appearance:none}.patch-slider.patch-scrollbar input[type="range"]::-webkit-slider-thumb{width:48px;height:14px;border:1px solid #71717a;border-radius:999px;background:#a1a1aa;-webkit-appearance:none}.patch-slider.patch-scrollbar input[type="range"]::-moz-range-thumb{width:48px;height:14px;border:1px solid #71717a;border-radius:999px;background:#a1a1aa}.patch-slider.patch-scrollbar input[type="range"]::-moz-range-track{height:16px;border:0;border-radius:999px;background:#e4e4e7}.patch-slider.patch-scrollbar>.patch-slider-value,.patch-slider.patch-scrollbar>.patch-slider-range{position:absolute!important;width:1px!important;height:1px!important;margin:-1px!important;padding:0!important;border:0!important;clip:rect(0 0 0 0)!important;clip-path:inset(50%)!important;overflow:hidden!important;white-space:nowrap!important}\n@media(prefers-color-scheme:dark){.patch-slider.patch-scrollbar input[type="range"]{background:#3f3f46}.patch-slider.patch-scrollbar input[type="range"]::-webkit-slider-thumb,.patch-slider.patch-scrollbar input[type="range"]::-moz-range-thumb{border-color:#a1a1aa;background:#71717a}}\n@media(forced-colors:active){.patch-slider.patch-scrollbar input[type="range"]{forced-color-adjust:auto}}\n</style>\`;\n}\n\nfunction scrollBarRuntime(ids) {\n  const idJson = JSON.stringify(ids).replace(/</g, '\\\\u003c');\n  return \`<script data-patch-window-scrollbar>\n(function(){\n  if(typeof renderControl!=='function'||typeof render!=='function')return;\n  const PATCH_SCROLLBAR_IDS=new Set(\${idJson});\n  const patchScrollBarOriginalRenderControl=renderControl;\n  renderControl=function(control,windowId,controlIndex){\n    const element=patchScrollBarOriginalRenderControl(control,windowId,controlIndex);\n    const id=String(control?.id||'');\n    if(control?.type!=='slider'||!PATCH_SCROLLBAR_IDS.has(id)||!element)return element;\n    element.classList?.add('patch-scrollbar');\n    element.dataset.patchSliderPresentation='scrollbar';\n    const input=element.querySelector?.('input[type="range"]');\n    if(input){input.setAttribute('aria-label',(id||'ScrollBar')+' scrollbar');input.setAttribute('aria-roledescription','scroll bar');}\n    return element;\n  };\n  render();\n})();\n</script>\`;\n}\n\nfunction paintBoxStyle() {`, 'Web scrollbar style/runtime insertion');
  return s;
});

edit('tests/progressbar-stage1.test.js', source => source
  .replace("assert.equal(PATCH_SLIDER_PRESENTATION_VERSION, '0.1');", "assert.equal(PATCH_SLIDER_PRESENTATION_VERSION, '0.2');")
  .replace("assert.equal(PATCH_WINDOW_SLIDER_PRESENTATION_VERSION, '0.1');", "assert.equal(PATCH_WINDOW_SLIDER_PRESENTATION_VERSION, '0.2');"));

const scrollBarTest = `import test from 'node:test';\nimport assert from 'node:assert/strict';\nimport fs from 'node:fs';\nimport { compile } from '../src/compiler.js';\nimport { buildStandaloneWebApp } from '../src/webapp.js';\nimport { buildCurrentNativeGuiIR } from '../src/native-current-contract.js';\nimport { validateWindowRuntimeSupport } from '../src/window-build.js';\nimport {\n  PATCH_SLIDER_PRESENTATION_VERSION,\n  PATCH_WINDOW_SLIDER_PRESENTATION_VERSION,\n  assertPatchSliderPresentationTarget,\n  parsePatchSliderPresentationDirective,\n  patchSliderPresentationTargetSupport,\n  readWindowSliderPresentation,\n  setWindowSliderPresentation\n} from '../src/slider-presentation.js';\n\nconst SOURCE = \`window "Scroll" as main size 520, 220:\n  # @slider-mode scrollbar\n  slider 0..100 as viewport step 5 at 24, 72 size 360, 28\n\nwhen viewport changed:\n  show value\n\`;\n\ntest('ScrollBar Stage 1 widens the Slider presentation contract to 0.2', () => {\n  assert.equal(PATCH_SLIDER_PRESENTATION_VERSION, '0.2');\n  assert.equal(PATCH_WINDOW_SLIDER_PRESENTATION_VERSION, '0.2');\n  assert.equal(parsePatchSliderPresentationDirective('# @slider-mode scrollbar'), 'scrollbar');\n  assert.deepEqual(patchSliderPresentationTargetSupport('scrollbar'), { studio: 'supported', web: 'supported', windows: 'unsupported', macos: 'unsupported', linux: 'unsupported', freebsd: 'unsupported' });\n  assert.doesNotThrow(() => assertPatchSliderPresentationTarget('scrollbar', 'web'));\n  assert.throws(() => assertPatchSliderPresentationTarget('scrollbar', 'windows'), /ScrollBar Stage 1 is Studio\\/Web only/);\n});\n\ntest('ScrollBar metadata round-trips on ordinary Slider source', () => {\n  const plain = \`window "Scroll" as main:\n  # @layout anchor left right\n  # @taborder 2\n  # @locked\n  slider 0..100 as viewport step 5\n\`;\n  const line = plain.split('\\n').findIndex(row => /^\\s*slider\\b/.test(row)) + 1;\n  const next = setWindowSliderPresentation(plain, line, 'scrollbar');\n  assert.match(next, /# @locked\\n  # @slider-mode scrollbar\\n  slider/);\n  assert.equal(readWindowSliderPresentation(next, line + 1), 'scrollbar');\n  assert.doesNotMatch(setWindowSliderPresentation(next, line + 1, 'plain'), /@slider-mode/);\n});\n\ntest('compiler keeps ScrollBar as Slider and Change IR 0.10', () => {\n  const compiled = compile(SOURCE, { name: 'Scroll', kind: 'window' });\n  const slider = compiled.ast.find(node => node.kind === 'window').body.find(node => node.control === 'slider');\n  assert.equal(compiled.ir.version, '0.10');\n  assert.equal(slider.sliderPresentation, 'scrollbar');\n  assert.equal(compiled.windowSliderPresentation.version, '0.2');\n  assert.equal(compiled.windowSliderPresentation.controls[0].mode, 'scrollbar');\n  const irSlider = compiled.ir.instructions.find(node => node.code === 'WINDOW').body.find(node => node.code === 'UI_CONTROL');\n  assert.equal(irSlider.control, 'slider');\n  assert.equal(Object.hasOwn(irSlider, 'sliderPresentation'), false);\n});\n\ntest('ScrollBar preserves ordinary Slider changed(value) and target gating', () => {\n  const compiled = compile(SOURCE, { name: 'Scroll', kind: 'window' });\n  assert.throws(() => validateWindowRuntimeSupport(compiled, { allowSlider: true }), /ScrollBar Stage 1.*Standalone Window Web.*no standalone ScrollBar presentation contract/i);\n  const web = validateWindowRuntimeSupport(compiled, { allowSlider: true, allowScrollBar: true });\n  assert.equal(web.sliders, 1);\n  assert.equal(web.scrollBars, 1);\n  assert.equal(web.events, 1);\n});\n\ntest('Current Ready native fails closed instead of lowering ScrollBar as Slider', () => {\n  const compiled = compile(SOURCE, { name: 'Scroll', kind: 'window' });\n  assert.throws(() => buildCurrentNativeGuiIR(compiled), /ScrollBar Stage 1.*Studio\\/Web only.*no standalone scrollbar presentation contract/i);\n});\n\ntest('Standalone Web renders interactive ScrollBar styling while retaining range input change semantics', () => {\n  const built = buildStandaloneWebApp(SOURCE, { name: 'Scroll', kind: 'window' });\n  assert.equal(built.metadata.scrollBarStage, 1);\n  assert.equal(built.metadata.scrollBarMode, 'interactive-slider-scrollbar-presentation');\n  assert.equal(built.metadata.scrollBarEvent, 'changed-number');\n  assert.match(built.html, /data-patch-window-scrollbar/);\n  assert.match(built.html, /patch-scrollbar/);\n  assert.match(built.html, /aria-roledescription','scroll bar'/);\n  assert.match(built.html, /safeTrigger\\(control\\.id,'changed',\\{value:Number\\(input\\.value\\)\\}\\)/);\n});\n\ntest('Patch Studio exposes ScrollBar as Slider preset and Inspector mode', () => {\n  const studio = fs.readFileSync('web/slider-stage1.js', 'utf8');\n  assert.match(studio, /addScrollBar/);\n  assert.match(studio, /\\+ ScrollBar/);\n  assert.match(studio, /option value="scrollbar">ScrollBar/);\n  assert.match(studio, /setWindowSliderPresentation\\(next, line, 'scrollbar'\\)/);\n  assert.match(studio, /patch-scrollbar-studio/);\n});\n`;
fs.writeFileSync('tests/scrollbar-stage1.test.js', scrollBarTest);

edit('docs/RAD_STUDIO_MASTER_BACKLOG.md', source => once(source, '- [ ] standalone ScrollBar where useful.', '- [x] standalone ScrollBar Stage 1: ordinary Slider plus `# @slider-mode scrollbar`, interactive Studio/Web scrollbar presentation preserving numeric `changed(value)` semantics, Current Ready native unsupported/fail-closed;', 'backlog scrollbar completion'));
edit('docs/ROADMAP.md', source => once(source, '- [x] ProgressBar as passive number-backed Slider presentation via `# @slider-mode progress`, Studio/Web supported with no control event and Current Ready native fail-closed', '- [x] ProgressBar as passive number-backed Slider presentation via `# @slider-mode progress`, Studio/Web supported with no control event and Current Ready native fail-closed\n- [x] ScrollBar as interactive Slider presentation via `# @slider-mode scrollbar`, preserving numeric `changed(value)` semantics in Studio/Web with Current Ready native fail-closed', 'roadmap scrollbar entry'));
edit('docs/STUDIO_AUTHORING_SURFACE.md', source => once(source, '- ProgressBar as number-backed Slider plus `# @slider-mode progress`;', '- ProgressBar as number-backed Slider plus `# @slider-mode progress`;\n- ScrollBar as ordinary interactive Slider plus `# @slider-mode scrollbar`, preserving numeric `changed(value)` and explicit persistence;', 'authoring scrollbar entry'));
edit('README.md', source => once(source, 'Calendar, LinkLabel, CheckedListBox, ProgressBar, GroupBox, ScrollBox, and SplitContainer', 'Calendar, LinkLabel, CheckedListBox, ProgressBar, ScrollBar, GroupBox, ScrollBox, and SplitContainer', 'README scrollbar surface'));
edit('web/index.html', source => once(source, 'PasswordEdit, MaskedEdit, NumberEdit, DatePicker, TimePicker and Calendar source-backed Input presentations plus LinkLabel Button presentation on Studio/Web;', 'PasswordEdit, MaskedEdit, NumberEdit, DatePicker, TimePicker and Calendar source-backed Input presentations plus LinkLabel Button and ScrollBar Slider presentations on Studio/Web;', 'public site scrollbar surface'));

edit('examples/patch-studio-showcase/main.patch', source => once(source, 'create number completion = 35\n', 'create number completion = 35\ncreate number scroll_position = 25\n', 'showcase scrollbar state'));
edit('examples/patch-studio-showcase/forms.patch', source => {
  let s = source;
  s = once(s, '    row "ProgressBar", "Passive Slider", "Studio/Web"\n    row "GroupBox",', '    row "ProgressBar", "Passive Slider", "Studio/Web"\n    row "ScrollBar", "Interactive Slider presentation", "Studio/Web"\n    row "GroupBox",', 'showcase scrollbar row');
  s = once(s, '        node "Slider"\n      node "Data"', '        node "Slider"\n        node "ScrollBar"\n      node "Data"', 'showcase scrollbar tree');
  s = once(s, '    button "Split Lab" as gallery_split at 18, 264 size 150, 40\n    button "Calendar Lab" as gallery_calendar at 182, 264 size 148, 40', '    button "Split Lab" as gallery_split at 18, 264 size 150, 40\n    button "Calendar Lab" as gallery_calendar at 182, 264 size 148, 40\n    # @slider-mode scrollbar\n    slider 0..100 as scroll_position step 5 at 18, 314 size 312, 28', 'showcase scrollbar control');
  return s;
});
edit('examples/patch-studio-showcase/logic.patch', source => source + '\nwhen scroll_position changed:\n  change scroll_position:\n    set = value\n  change gallery_status:\n    set = "ScrollBar position updated"\n');
edit('examples/patch-studio-showcase/README.md', source => once(source, '- ProgressBar as the passive number-backed Slider `# @slider-mode progress` presentation;\n', '- ProgressBar as the passive number-backed Slider `# @slider-mode progress` presentation;\n- ScrollBar as the interactive Slider `# @slider-mode scrollbar` presentation with numeric `changed(value)`;\n', 'showcase README scrollbar'));

edit('tests/studio-showcase.test.js', source => {
  let s = source;
  s = once(s, '  assert.match(composition.source, /# @slider-mode progress/);', '  assert.match(composition.source, /# @slider-mode progress/);\n  assert.match(composition.source, /# @slider-mode scrollbar/);', 'showcase source scrollbar');
  s = once(s, "  assert.equal(compiled.windowSliderPresentation.controls.some(control => control.mode === 'progress'), true);", "  assert.equal(compiled.windowSliderPresentation.controls.some(control => control.mode === 'progress'), true);\n  assert.equal(compiled.windowSliderPresentation.controls.some(control => control.mode === 'scrollbar' && control.id === 'scroll_position'), true);", 'showcase compiled scrollbar');
  s = once(s, "  assert.equal(built.metadata.progressBarStage, 1);", "  assert.equal(built.metadata.progressBarStage, 1);\n  assert.equal(built.metadata.scrollBarStage, 1);\n  assert.equal(built.metadata.scrollBarEvent, 'changed-number');", 'showcase web scrollbar metadata');
  s = once(s, '  assert.match(built.html, /patch-progressbar/);', '  assert.match(built.html, /patch-progressbar/);\n  assert.match(built.html, /patch-scrollbar/);', 'showcase web scrollbar html');
  return s;
});
edit('tests/docs-current-studio-surface.test.js', source => once(source, '  assert.match(roadmap, /ProgressBar as passive number-backed Slider/);', '  assert.match(roadmap, /ProgressBar as passive number-backed Slider/);\n  assert.match(roadmap, /ScrollBar as interactive Slider presentation/);', 'docs scrollbar acceptance'));

const projectPath = 'examples/patch-studio-showcase.patchproject';
const bundle = parseStudioProjectBundle(fs.readFileSync(projectPath, 'utf8'));
for (const file of bundle.files) file.content = fs.readFileSync(`examples/patch-studio-showcase/${file.path}`, 'utf8');
const canonical = serializeStudioProjectBundle(bundle);
if (canonical.includes('`')) throw new Error('Showcase canonical JSON unexpectedly contains a backtick.');
fs.writeFileSync(projectPath, canonical);
const embeddedPath = 'web/studio-showcase-project.js';
const embedded = fs.readFileSync(embeddedPath, 'utf8');
const marker = 'export const PATCH_STUDIO_SHOWCASE_PROJECT = String.raw`';
const start = embedded.indexOf(marker);
if (start < 0) throw new Error('Showcase embedded-project marker is missing.');
const contentStart = start + marker.length;
const end = embedded.indexOf('`;', contentStart);
if (end < contentStart) throw new Error('Showcase embedded-project terminator is missing.');
fs.writeFileSync(embeddedPath, embedded.slice(0, contentStart) + canonical + embedded.slice(end));

console.log('ScrollBar Stage 1 patch applied.');
